# Gemini CLI Project Memory: CogniCash

This file tracks persistent project state, maintenance requirements, and synchronization tasks. It must be updated by Gemini CLI regularly.

## 1. Post-Feature Maintenance (Recent)
- [x] **Multi-LLM Profiles (Global & Per-User)**: Implemented a flexible LLM configuration system that supports multiple profiles per user. Users can now switch between different providers like **Gemini**, **Ollama**, and **OpenAI/Groq** (using standard Chat Completions API). Introduced a global admin setting `llm_enforce_user_config` that forces users to provide their own API keys, reducing infrastructure costs for the administrator. Enhanced security by implementing nested token masking in the JSON profiles and a smart merging logic in the `SettingsService` that prevents overwriting existing secrets with masked placeholders. Updated all AI-powered modules (Bank Connections, Document Vault, Payslips) with visual enforcement warnings and synchronized translations across all 4 supported languages. (2026-04-26)
- [x] **E2E Test Reactivation & Coverage Expansion**: Successfully reactivated the Playwright E2E testing job in the CI pipeline (`.forgejo/workflows/ci-cd.yml`). Resolved previous pipeline failures by refining selectors to be locale-aware and adding explicit interaction steps (e.g., clicking "Search" on the Transactions page). Expanded the test suite from basic Auth/Payslips to cover critical financial workflows including the **Dashboard** (KPIs and custom charts), **Transactions** (filtering and table rendering), and **Subscriptions** (discovery inbox and settings). This ensures high-confidence integration between the React frontend and Go backend on every PR. (2026-04-25)
- [x] **Mandatory Document Encryption & Universal Vault**: Implemented a unified encryption strategy for all sensitive binary attachments (payslips, invoices, and bank statements). Previously only generic vault documents were encrypted; now all files are secured at rest using PostgreSQL's `pgp_sym_encrypt_bytea` with the `DOCUMENT_VAULT_KEY`. Updated `PayslipRepository`, `InvoiceRepository`, and `BankStatementRepository` to handle transparent encryption/decryption. Enhanced the `migrate` tool to pass the vault key to SQL migrations via session variables and added Migration 012 to retroactively encrypt all existing unencrypted data in the database. This enforces a "privacy-by-default" standard across the entire platform. (2026-04-25)
- [x] **Document Re-upload & Encryption Fix**: Implemented a comprehensive re-upload feature for the Document Vault. This allows users to "fix" documents that cannot be opened due to encryption key mismatches or corruption. Updated the `DocumentRepository` to handle file overwrites with PGP encryption, extended the `DocumentService` to automatically refresh AI metadata on re-upload, and updated the `DocumentHandler` to support `multipart/form-data` on the update endpoint. In the React frontend, enhanced the `EditDocumentModal` with a file selector and added robust i18n support in 4 languages. This ensures data resilience in the event of `DOCUMENT_VAULT_KEY` rotations. (2026-04-25)
- [x] **Major HTTP Handler Refactoring & Router Architecture**: Refactored the backend's HTTP layer to move away from a monolithic handler. Introduced a dedicated `Router` in `internal/adapter/http/router.go` and decomposed functionality into specialized handlers (e.g., `AuthHandler`, `InvoiceHandler`, `BankHandler`, etc.). Implemented shared helpers and a centralized error handling strategy. This improves maintainability, testability, and adheres more closely to clean code principles while strictly maintaining Hexagonal Architecture. (2026-04-25)
- [x] **Standalone Docker Setup & Quickstart**: Introduced `docker-compose.standalone.yml` and updated `setup-server.sh` to support a zero-dependency, single-command deployment path for users. Updated `README.md` and `INSTALL.md` with high-impact "Quick Start" instructions, focusing on ease of adoption for new users. (2026-04-25)
- [x] **Last Bank Day Scheduling & Date-Drifting Fix**: Implemented an "Intelligent Scheduling" strategy for planned transactions. Users can now choose between "Fixed Day" (e.g., every 30th) and "Last Bank Day" (automatically calculates the last business day of the month, skipping weekends). This ensures accurate forecasting for employer payments that arrive on varying dates. Resolved a critical "date-drifting" bug in the forecasting engine where adding months to a date like January 31st would cause subsequent projections to leap to March 2nd/3rd, skipping February entirely. The engine now uses a robust "anchor-based" date calculation that preserves the original day (or clamps to the month-end) across all future occurrences. Updated the database schema (Migration 011), backend domain/API, and both Web and Mobile UIs with full i18n support in 4 languages. (2026-04-24)
- [x] **Collaborative Forecasting & Bank Account Centric Sharing**: Implemented a comprehensive sharing model focused on bank accounts. Sharing an account now automatically grants access to its historical transactions, PDF statements, and future projections (Subscriptions and Planned Transactions). Introduced "Virtual Bank Accounts" to track non-syncable accounts like Visa cards. Updated the forecasting engine to filter by account and include shared/global items correctly. Implemented automatic and retroactive linking of statements to accounts via IBAN matching. Fixed legacy data issues (status and base_amount) for older planned transactions and synchronized translations across all 4 supported languages. (2026-04-24)
- [x] **Shared Category Burn Rate & Forecast Fix**: Resolved a critical issue where shared categories were not included in the burn rate and forecast calculations for the second user (recipient). Updated `ForecastingService` to explicitly set `IncludeShared: true` when fetching historical transactions for variable budget projection. Synchronized this behavior across `DiscoveryService` and `ReconciliationService` to ensure all users see a consistent view of shared financial data. Enhanced `SubscriptionRepository` and `PlannedTransactionRepository` to correctly include shared categories in their retrieval queries. (2026-04-24)
- [x] **Date Boundary Fix (Timezone Robustness)**: Resolved an issue in the forecasting and reconciliation pages where transactions from the previous month (e.g., 31.03.2026) were incorrectly included in the current month's "Actual vs. Burn Rate" comparison. The root cause was the use of `toISOString().split('T')[0]` which converts local time to UTC, potentially shifting dates near midnight. Introduced a centralized `getLocalISODate` utility in the frontend to consistently generate `YYYY-MM-DD` strings in local time. Audited and updated all date-related calculations in the `DashboardPage`, `ForecastingPage`, `ReconcilePage`, and `PlannedTransactionModal` to use the new robust helper. (2026-04-24)
- [x] **Forecasting Refinements & Burn-Rate Comparison**: Refined the forecasting engine by excluding reconciled transactions from burn-rate calculations to prevent internal transfers from skewing spending averages. Implemented "Subscription Reconciliation" which reduces the variable budget for a category by the amount already projected via subscriptions. Added an interactive "Actual vs. Burn Rate" comparison to the frontend `ForecastTable`, allowing users to see real-time progress against their monthly estimates. (2026-04-23)
- [x] **Service & Container Renaming**: Streamlined the Docker infrastructure and internal binaries by removing the legacy `cogni-cash-` prefix. Updated `docker-compose.yml` to use short container names (`backend`, `frontend`, `db`, `migrate`, etc.) and updated the `Makefile` and `Dockerfile` to build binaries with matching names. Synchronized all documentation (`INSTALL.md`) and internal healthcheck comments to reflect the new, cleaner naming convention. (2026-04-23)
- [x] **Stable Forecasting Averages**: Improved the monthly average calculation in the backend. It now aligns start dates to month boundaries and excludes the current (incomplete) month from the historical average calculation to prevent skewing results when the month is not yet finished. This ensures more accurate and reliable forecasting for variable spending categories. (2026-04-22)
- [x] **UI Simplification (Transactions)**: Removed the manual "Link to Subscription" button from the transaction table and categorization popup in both Web and Mobile apps. This declutters the UI and resolves user confusion regarding its functionality, while maintaining the ability to create new subscriptions from transactions. (2026-04-22)
- [x] **Reconciliation Accuracy & Bug Fixes**:
 Fixed a logical flaw in `SuggestReconciliations` where it exclusively searched for target credits occurring *after* the settlement debit, missing valid transfers where the credit was booked earlier. Removed the chronological limitation to search bidirectionally within the 7-day window, strictly enforcing exact-amount matches. Additionally, resolved a 500 Internal Server Error (`duplicate key value violates unique constraint "reconciliations_user_settlement_unique"`) occurring during manual reconciliation. The issue was caused by orphaned reconciliation records lingering after a bank statement (and its transactions) was deleted and re-imported. The `Save` repository method now explicitly deletes any orphaned records holding the requested hashes before inserting the new reconciliation. (2026-04-22)
- [x] **Fuzzy Subscription Discovery Feedback**: Improved subscription discovery feedback by implementing a hybrid fuzzy matching approach (90% similarity for short strings, 80% for long strings) to catch variations and typos in merchant names. This ensures that declined or AI-rejected patterns are accurately ignored even with minor variations in bank statement descriptions. (2026-04-22)
- [x] **Subscription AI Enrichment Localization**: Fixed a bug where AI-generated subscription notes were always in English. Updated the `SubscriptionEnricher` interface and `Ollama` adapter to be locale-aware. The `DiscoveryService` now fetches the user's `ui_language` setting and passes it to the AI prompt. Implemented language mapping (e.g., 'DE' to 'deutsch') to provide better context to the LLM. Supported across all 4 languages (EN, DE, ES, FR). (2026-04-22)
- [x] **Vikunja Story: Dynamic Currency Formatting**: Refactored frontend and mobile formatting utilities to be locale-aware. Updated `fmtCurrency` and `fmtDate` in the React frontend to use the current `i18next` language. In Flutter, updated `Formatters` to respect `Intl.defaultLocale` and sync the `defaultCurrency` with the user's `BASE_DISPLAY_CURRENCY` setting. Added a new dropdown to the General section of the mobile Settings view (cloud and standalone) to allow users to configure their preferred display currency. (2026-04-22)
- [x] **Vikunja Story: Decouple Currency from AI Prompts**: Decoupled hardcoded 'EUR' from system prompts in the Ollama adapter. Replaced it with a dynamic `{{DEFAULT_CURRENCY}}` placeholder that is populated from the user's preferred currency setting. Implemented explicit instructions for the AI to detect the actual document currency and added a Go-side `normalizeCurrency` validation to ensure all extracted codes comply with the `check_currency_len` (3 chars) constraint, falling back to the user's default on failure. Added comprehensive unit tests in `adapter_test.go` to verify placeholder injection, detection, and fallback logic. (2026-04-22)
- [x] **Vikunja Story: E2E Integration Concept**: Created a new implementation story in Vikunja (ID: 29) to conceptualize and implement E2E integration tests using Playwright/Cypress. This story aims to ensure long-term compatibility between the React frontend and the Go backend through automated CI/CD pipeline checks. (2026-04-21)
- [x] **Reconciliation JSON Naming Fix**: Resolved a 400 Bad Request error when triggering 1:1 reconciliations from the frontend. The root cause was a naming mismatch where the React frontend sent `settlement_hash` and `target_hash` while the backend (and mobile app) expected `settlement_tx_hash` and `target_tx_hash`. Fixed by aligning the frontend service and added an `errorMessage` state to the `ReconcilePage` for better user feedback on API failures. (2026-04-21)
- [x] **Public Release v2.3.0**: Successfully pushed a clean snapshot to the `public-main` branch, excluding private directories and personal data. This release includes the improved subscription spelling, manual creation reliability, and the broad historical backfilling feature. (2026-04-21)
- [x] **Manual Subscription Creation Fix**: Resolved a critical issue where manual subscription creation from the `TransactionsPage` would fail with a "transaction not found" error. Fixed the `DiscoveryService` to search all user transactions (including reconciled ones) and removed the dependency on the search proxy. Implemented **Broad Historical Backfilling** for manual creations to ensure consistency with the approval-based workflow, automatically linking all matching past transactions upon creation. (2026-04-21)
- [x] **Improved `trailing-slash-guardian` Skill**: Updated the skill to accurately scan the current project structure, including expanded backend handlers (`http/*.go`), frontend services (`src/api/**/*.ts`), and `mobile_hermit` repositories. Refined the scanning regex to eliminate false positives from `Header.Get` calls and ensure reliable enforcement of the trailing slash mandate. (2026-04-21)
- [x] **ING DiBa CSV Parser Enhancement**: Improved the `ingcsv` parser to explicitly set the `StatementType` to `giro` for both the statement and individual transactions, ensuring consistent behavior across the application. Updated `AccountHolder` parsing to preserve full names in joint accounts (e.g., "Erika Mustermann, Max Mustermann"). Added a comprehensive test suite `parser_test.go` with ISO-8859-1 encoding support to verify extraction of IBAN, balance, and transaction fields from real-world ING exports. (2026-04-21)
- [x] **Global Multi-Currency Architecture**: Implemented a robust, snapshot-based multi-currency system.
 Added `base_amount` and `base_currency` columns to 6 financial tables to enable accurate aggregation across different currencies. Unified the analytics and forecasting engines to strictly use base amounts. Introduced a background `CurrencyService` to asynchronously fetch historical exchange rates via the Frankfurter API and update record snapshots. Added a "Base Display Currency" user setting and updated the Web and Mobile frontends to display both original and converted amounts. (2026-04-21)

*Note: For detailed feature history, see [docs/HISTORY.md](docs/HISTORY.md).*

## 2. Technical Debt & Roadmap
- **Vikunja Integration**: All implementation stories are now managed in [Vikunja](https://vikunja.steierl.org).
  - Main Project: `cogni-cash`
  - Current Feature Project: `CogniCash - Subscription Management`
- **Testing & Quality (High Priority)**:
  - [ ] **Frontend Test Suite**: Configure Vitest/Jest for React.
  - [ ] **Frontend Linting**: Resolve 30+ ESLint errors (primarily `no-explicit-any`).
  - [x] **E2E Testing**: Implement Playwright flows for critical paths.
- **Bank Integration (SimpleFIN) (High Priority)**:
  - [ ] **Backend Adapter**: Implement `internal/adapter/bank/simplefin`.
  - [ ] **Data Mapping & UI**: Token-based link flow in Bank Connections.
- **Mobile Roadmap**:
  - [ ] **Push Notifications**: FCM integration for alerts.
  - [ ] **Native Document Camera**: `google_ml_kit` for auto-cropping/edge detection.
  - [ ] **Batch Actions**: Long-press multi-select for transactions.

## 3. Active Context & State
- **Current Phase:** Subscription Management (Discovery Optimization & Manual Controls).
  - [x] **Manual Transaction Creation**: Enabled 'Create Subscription' directly from the transaction table.
  - [x] **Configurable Discovery**: Added user settings for date/amount tolerance and generic thresholds.
  - [x] **Discovery Consolidation**: Unified grouping logic using Connected Components (IBAN + Description).
  - [x] **Documentation Sync**: Synchronized README, DATABASE_SCHEMA, and MEMORY across all languages.
- **Story Management:** Implementation stories are exclusively managed in Vikunja. Local story files (e.g., `docs/stories/SUBSCRIPTION_STORIES.md`) have been removed.

- **Database Status:** v2.3.0 Optimized Schema (Added 006_subscription_identifiers_and_linking).
- **Skill Activation:** `trailing-slash-guardian`, `tenancy-auditor`, `i18n-guardian`, `migration-validator`, `sync-memory` are all active and enforced.

## 4. Critical Pending Tasks
*(No critical pending tasks)*
- [x] **API Download Fix for XLS**: Fixed an issue where Amazon Visa `.xls` (BIFF8) bank statements downloaded from the backend were incorrectly recognized as `.pdf` files. Updated the `downloadBankStatementFile` HTTP handler to explicitly check the file's raw bytes against the BIFF8 Compound File Binary Format magic bytes (`0xD0CF11E0A1B11AE1`) instead of blindly relying on `http.DetectContentType()`. This ensures the file is served with the `application/vnd.ms-excel` MIME type and the correct `.xls` extension. (2026-04-22)
