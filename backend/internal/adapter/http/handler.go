package http

// This file is intentionally left mostly empty after the refactoring to a decomposed 
// Router architecture (see router.go and individual *_handler.go files).
// It remains here to avoid breaking any potential external references during the transition.
